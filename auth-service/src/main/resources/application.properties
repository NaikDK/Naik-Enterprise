spring.application.name=auth-service

server.port=${SERVER_PORT_AUTH:8080}
server.servlet.context-path=/auth

spring.mongodb.uri=${MONGODB_URI}
spring.data.mongodb.auto-index-creation=true

# JWT
jwt.secret=${JWT_SECRET}